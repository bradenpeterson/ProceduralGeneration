using System;

public static class BinaryGenerator
{
	public static int[,] Generate(
		int width,
		int height,
		int minDepth,
		int maxDepth,
		float splitChance,
		int? seed = null)
	{
        pass;
	}
}